- name: Hello world
  hosts: all
  gather_facts: no
  tasks:
    - name: Wait for connection
      wait_for_connection:
        timeout: 600

    - name: Gather facts
      setup:

    - name: Gather services facts
      service_facts:
  
    - name: Print hello world
      debug:
        msg: "Hello World"
roles:
    - role: user-access
      user_access_username: user
      user_access_password: 'Unknown@Admin345'
      user_access_sudo: true
